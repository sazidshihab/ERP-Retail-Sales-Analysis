name: Test EC2 Connection  # <-- top-level mapping, not a list

on:
  workflow_dispatch:       # allows manual trigger

jobs:
  test-connection:
    runs-on: ubuntu-latest
    steps:
      - name: Check SSH port
        run: |
          nc -zv EC2_PUBLIC_IP 22 || echo "Cannot connect to SSH"

